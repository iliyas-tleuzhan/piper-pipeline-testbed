# Future Bunker Integration

This repository does not implement actual Bunker movement. It provides `lock_base`, `unlock_base`, `is_base_stopped`, `request_navigation_pause`, `navigation_ready`, and `get_navigation_direction` for later implementation.
